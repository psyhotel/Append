# 🎤 VoiceNotes MVP - Умные голосовые заметки

## 📋 Обзор проекта
VoiceNotes - это приложение для записи и категоризации голосовых заметок с AI-анализом и системой напоминаний. Создано на Kotlin + Ktor (backend) и vanilla JavaScript (frontend).

## ✨ Основные возможности
- 🎙️ **Запись аудио** через браузер (Web Audio API)
- 🔤 **Транскрибация** аудио в текст (OpenAI Whisper)
- 📊 **AI бизнес-отчеты** по каждой идее (OpenAI GPT)
- 📁 **Категоризация** заметок
- 🔔 **Напоминания** с календарем
- ✏️ **Редактирование** заметок и транскрипций
- 🎨 **Неоновый дизайн** (фиолетово-синий)

## 🏗️ Архитектура

### Backend (Kotlin + Ktor)
- **Framework**: Ktor 2.3.7
- **Language**: Kotlin 1.9.22
- **Port**: 5000
- **Storage**: In-memory (InMemoryStorage) - для MVP
- **API**: REST endpoints в `/api/*`

### Frontend
- **Technology**: Vanilla JavaScript + HTML + CSS
- **Design**: Неоновый стиль с градиентами
- **Features**: Web Audio API для записи, fetch API для взаимодействия с backend

### Структура проекта
```
src/main/kotlin/com/voicenotes/
├── Application.kt              # Точка входа, конфигурация Ktor
├── models/
│   └── Models.kt               # Модели данных (VoiceNote, Category, Reminder)
├── storage/
│   └── InMemoryStorage.kt      # In-memory хранилище
├── services/
│   └── OpenAIService.kt        # Сервис для AI транскрибации и отчетов
└── routes/
    └── Routes.kt               # REST API endpoints

src/main/resources/frontend/static/
├── index.html                  # Главная страница
├── style.css                   # Неоновые стили
└── app.js                      # Frontend логика
```

## 🔌 API Endpoints

### Заметки
- `GET /api/notes` - Получить все заметки
- `POST /api/notes` - Создать новую заметку
- `GET /api/notes/{id}` - Получить заметку по ID
- `PATCH /api/notes/{id}` - Обновить заметку
- `DELETE /api/notes/{id}` - Удалить заметку

### Транскрибация и AI
- `POST /api/transcribe` - Загрузить аудио и транскрибировать
- `POST /api/generate-report/{noteId}` - Создать AI бизнес-отчет

### Категории
- `GET /api/categories` - Получить категории
- `POST /api/categories` - Создать категорию

### Напоминания
- `GET /api/reminders` - Получить все напоминания
- `POST /api/reminders` - Создать напоминание
- `PATCH /api/reminders/{id}` - Обновить статус напоминания

## 🚀 Запуск проекта

### Требования
- Java 17+
- Maven

### Команда запуска
```bash
mvn exec:java
```

Приложение запустится на http://localhost:5000

## 🔑 Настройка OpenAI (опционально)

Для реальной транскрибации и AI-отчетов добавьте переменную окружения:
```
OPENAI_API_KEY=your_key_here
```

Без API ключа приложение работает в демо-режиме с примерами.

## 🎨 Дизайн
- **Цветовая схема**: Градиенты фиолетового (#8B5CF6), синего (#3B82F6), голубого (#06B6D4)
- **Эффекты**: Неоновое свечение, анимации мерцания, пульсация
- **Отзывчивость**: Адаптивный дизайн для мобильных устройств

## 📝 Категории по умолчанию
1. Бизнес идеи (фиолетовый)
2. Задачи (синий)
3. Мысли (голубой)
4. Важное (розовый)

## 🔄 Дальнейшее развитие
- [ ] Интеграция с PostgreSQL для персистентного хранения
- [ ] Реальная интеграция с OpenAI Whisper API
- [ ] Экспорт в календари (Google Calendar, iCal)
- [ ] Поиск по заметкам
- [ ] Теги и фильтры
- [ ] Аутентификация пользователей
- [ ] Мобильное приложение

## 📅 История изменений

### 2025-11-10 - Initial MVP
- Создан Kotlin + Ktor backend
- Реализован frontend с неоновым дизайном
- Добавлена запись аудио через браузер
- Интегрирована система категорий и напоминаний
- Создан mock-сервис для AI отчетов
